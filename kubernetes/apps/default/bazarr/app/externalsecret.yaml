---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bazarr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: bazarr-secret
    template:
      data:
        BAZARR__POSTGRES_DATABASE: "{{ .POSTGRES_DATABASE }}"
        BAZARR__POSTGRES_HOST: "{{ .POSTGRES_HOST }}"
        BAZARR__POSTGRES_USER: "{{ .POSTGRES_USER }}"
        BAZARR__POSTGRES_PASS: "{{ .POSTGRES_PASS }}"
        BAZARR__POSTGRES_PORT: "{{ .POSTGRES_PORT }}"
        INIT_POSTGRES_DBNAME: "{{ .INIT_POSTGRES_DBNAME }}"
        INIT_POSTGRES_HOST: "{{ .INIT_POSTGRES_HOST }}"
        INIT_POSTGRES_USER: "{{ .INIT_POSTGRES_USER }}"
        INIT_POSTGRES_PASS: "{{ .INIT_POSTGRES_PASS }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .INIT_POSTGRES_SUPER_PASS }}"
  dataFrom:
    - extract:
        key: bazarr
